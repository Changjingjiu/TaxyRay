# TaxLens Android design

Authority: 用户提供的 UX风格规范文档.md，以及2026-09-15截图反馈。后者对冲突处优先。Mode: Operate。

保留 Modern Fiscal 的既定视觉，不另换设计方向。首页以累计增值税估算、消费金额和有效占比为核心，下面提供记录入口及按日期排序的票据列表。没有账本时展示真实空态，不预装假数据。

Material 3 负责安全区、输入、底栏、弹窗、键盘和系统返回。界面色彩以 #F8F9FA / #FFFFFF / #0F172A / #64748B / #047857 为浅色基线；深色沿用 #09090B / #18181B / #F8FAFC / #94A3B8 / #10B981。金额使用等宽系统字体，页面标题22sp，核心数字36sp，正文14sp。交互区域至少48dp。

票据虚线、无厚重阴影、12dp圆角；一层承载明细，避免多层卡片嵌套。单次关键反馈为税额数字位滚动，固定小数点，尊重系统关闭动画。表单和复核保持清楚的错误提示以及可以恢复的输入。

贡献卡保留票据式秩序、税额构成条和“非发票 / 非完税证明”说明。按截图反馈删除英文页眉、估算章及摘要编号；不修改用户自己的商户名称。点击生成时一次触觉与彩纸庆典，覆盖层不接收点击、不导出进图片，关闭系统动画时跳过彩纸。

总览去掉品牌顶栏；账本保留标题与新增入口。两页底部免责声明统一居中。设置页去掉顶部隐私宣传块，识别区标题“小票识别AI”，仅保留简短模型推荐及必要配置提示。

记账退出确认必须保持表单展开，只能通过“继续编辑”或“放弃修改”作出选择。点击对话框外和系统返回均不关闭确认，防止留下隐藏的模态触摸层。

第三轮反馈去掉空账本的标题和说明，仅保留插图与可用动作。消费说明与贡献卡文案使用短句、空格和换行；税率指南每档独立一行，类别可点击查看代表与条件。设置页以 GitHub 图标加 TaxyRay 链接项目，检查更新使用独立入口。
