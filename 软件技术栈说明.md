# 角色与目标
你是一名资深 Android 架构师。请使用 Kotlin + Jetpack Compose + Material 3 构建一个 100% 运行在客户端、注重个人隐私的离线优先应用——「消费税额明细与纳税贡献计算器」。

应用支持用户手动录入消费项，或通过自带 API Key（BYOK 模式）调用兼容 OpenAI 规范的多模态视觉大模型（如 DeepSeek-VL / Qwen-VL / GPT-4o 等），通过 Tool Calling（Function Calling）结构化提取小票明细与中国增值税档位，经用户二次人工核对后存入本地 SQLite (Room) 数据库，并支持生成极简风格的“纳税人贡献卡”分享图。

---

## 1. 技术栈与工程规范

- **开发语言**：Kotlin 2.0+（启用 Coroutines + Flow）
- **UI 框架**：Jetpack Compose（Material 3 风格，纯声明式 UI，适配沉浸式边缘到边缘 Edge-to-Edge）
- **SDK 配置**：`minSdk = 26`, `targetSdk = 34/35`, `compileSdk = 35`
- **核心依赖**：
  - 本地存储：`androidx.room:room-runtime`, `androidx.room:room-ktx`, `androidx.room:room-compiler` (KSP)
  - 密钥安全：`androidx.security:security-crypto:1.1.0-alpha06`（EncryptedSharedPreferences）
  - 网络通讯：`com.squareup.okhttp3:okhttp:4.12.0`（用于多模态 API 请求）
  - JSON 解析：`org.jetbrains.kotlinx:kotlinx-serialization-json:1.6.3`
  - 异步图片加载：`io.coil-kt:coil-compose:2.6.0`
  - 图表/图标：`androidx.compose.material:material-icons-extended`

---

## 2. 税务计算与业务逻辑

### 2.1 增值税反推公式（价税分离）
中国国内零售实行“价税合一”，实付金额即为价税合计：
- `不含税金额 = 实付金额 / (1.0 + 税率)`
- `增值税额 = 不含税金额 * 税率`
- 金额计算一律使用 `BigDecimal` 保留两位小数，采用 `RoundingMode.HALF_UP`（四舍五入）。

### 2.2 预设税率字典
- **13% (标准税率)**：工业制成品、日化洗护、加工食品、数码家电、服装鞋帽。
- **9% (低税率)**：未经深加工的初级农产品、粮食、生鲜蔬菜瓜果、肉蛋水产、图书报刊。
- **0% (免税项)**：商超小票明确列为“免税/F”的生鲜商品。
- **自定义税率**：允许用户在界面上任意输入或选择（如特殊服务 6%）。

---

## 3. 核心功能模块与技术实现

### 3.1 BYOK 设置与安全存储 (`SettingsRepository`)
- 使用 `EncryptedSharedPreferences` 存储三项配置：
  1. `api_base_url`（默认 `https://api.deepseek.com/v1` 或通用兼容地址）
  2. `model_name`（默认例如 `deepseek-chat` 或指定视觉模型）
  3. `api_key`（加密存储，脱敏展示）

### 3.2 多模态 Tool Calling 小票解析器 (`ReceiptAgentService`)
1. **图像前置压缩**：
   - 手机拍照或相册选图后，在本地将图片等比缩放至长边最大 1920px。
   - 压缩为 JPEG（质量 80%），控制在 1MB 以内并转为 Base64 编码。
2. **API 请求构造（OpenAI-Compatible Chat Completions with Tools）**：
   - 请求体包含 System Prompt 与带有 `image_url`（Base64 Data URI）的 User Message。
   - **声明 Tool/Function**：
     ```json
     {
       "type": "function",
       "function": {
         "name": "record_receipt_items",
         "description": "从购物小票图片中结构化提取商品明细与对应的中国增值税率",
         "parameters": {
           "type": "object",
           "properties": {
             "store_name": { "type": "string", "description": "商户/超市名称" },
             "items": {
               "type": "array",
               "items": {
                 "type": "object",
                 "properties": {
                   "name": { "type": "string", "description": "商品名称" },
                   "amount": { "type": "number", "description": "实付小计金额(元)" },
                   "tax_rate": {
                     "type": "number",
                     "enum": [0.13, 0.09, 0.00],
                     "description": "适用的增值税率"
                   },
                   "category_reason": { "type": "string", "description": "分类说明" }
                 },
                 "required": ["name", "amount", "tax_rate"]
               }
             }
           },
           "required": ["items"]
         }
       }
     }
     ```
   - 强制使用 `tool_choice: {"type": "function", "function": {"name": "record_receipt_items"}}`。
3. **响应拦截**：
   - 解析返回的 `choices[0].message.tool_calls[0].function.arguments` JSON 字符串，转为本地临时数据对象。

### 3.3 人工复核工作流（Human-in-the-Loop）
- 模型解析成功后，**禁止直接静默入库**，必须在 Compose 中弹出一个 **可编辑底栏/弹窗（ModalBottomSheet）**：
  - 用户可勾选/删除错误识别项。
  - 点击商品条目可直接修改名称、金额或一键切换税率标签（13% / 9% / 0%）。
  - 用户确认无误后，点击“保存至账本”，触发 Room 事务写入。

### 3.4 数据库设计 (`AppDatabase`)
- **`ReceiptEntity`**：`id` (主键自增), `storeName` (商户名), `totalAmount` (总金额), `totalTaxAmount` (总税额), `timestamp` (时间戳)。
- **`ReceiptItemEntity`**：`id` (主键自增), `receiptId` (外键关联), `name` (品名), `amount` (实付金额), `taxRate` (税率), `taxAmount` (计算税额)。
- 提供获取总纳税额、近 30 天消费趋势的 DAO 查询接口。

### 3.5 纳税贡献卡生成与分享 (`ShareCardHelper`)
- 设计一个符合极简公文字体风格的 Compose 贡献卡片 UI：
  - 核心文案：“本次消费 ¥[总金额]，为城市建设与公共事业贡献了增值税 **¥[税额]**。每一笔理性消费，都在支撑国家运转。”
  - 包含时间、商户名、税负占比环形条或进度条。
- 提供将 Compose 组件渲染为 `android.graphics.Bitmap` 并调起系统原生分享（`Intent.ACTION_SEND`）的工具方法。

---

## 4. 输出要求

请以生产级质量提供完整的 Kotlin 代码，并按模块拆分到对应文件：
1. `data/local/TaxEntities.kt` & `data/local/ReceiptDao.kt`：完整的 Room 实体与 DAO。
2. `data/security/SecurePreferences.kt`：封装的加密存储模块。
3. `data/remote/VisionAgentService.kt`：OkHttp 请求构建、图片压缩与 Tool Call 参数解析。
4. `domain/TaxCalculator.kt`：单利价税分离纯函数工具类。
5. `ui/screens/ScannerReviewSheet.kt`：用户人工核对界面的 Compose 实现。
6. `ui/screens/DashboardScreen.kt`：包含历史累计贡献、消费记录列表的主界面。
7. `ui/components/TaxContributionCard.kt`：用于截图导出的凭证卡片组件及 Bitmap 导出逻辑。