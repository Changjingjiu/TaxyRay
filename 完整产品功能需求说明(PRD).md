# 产品需求文档 (PRD)：TaxLens (隐形税负透视镜)

| 文档版本 | 状态 | 编写日期 | 适用平台 |
| :--- | :--- | :--- | :--- |
| v1.0.0 | 待开发评审 | 2026-09 | 客户端离线优先 (Android / iOS / PWA 通用) |

---

## 一、 项目背景与产品目标

### 1.1 背景
中国大陆零售消费普遍采取“价税合一”的定价与开票模式，收银小票上仅展示最终支付总额，不单独列示税率与税额。绝大多数消费者在日常消费中并不感知自己承担的增值税份额，缺乏作为纳税人的直观体验。

### 1.2 产品定位
一款**注重个人隐私、纯客户端离线运行**的消费税额透视与记账辅助工具。
- **核心价值**：帮助用户将日常消费账单进行“价税分离”，透视隐藏在商品价格中的增值税贡献。
- **设计哲学**：
  1. **零中心化依赖**：数据 100% 留存在用户设备沙盒内，无服务器、无埋点、无远程同步。
  2. **端侧优先与 BYOK**：支持手动记账；AI 识别采用用户自带 API Key (Bring Your Own Key) 模式，客户端直连大模型。
  3. **正向叙事**：以“公民纳税贡献”为情感内核，规避对抗性情绪，强化建设者认同感。

---

## 二、 用户画像与核心使用场景

| 角色 | 典型特征 | 核心场景 | 核心诉求 |
| :--- | :--- | :--- | :--- |
| **公民意识觉醒者** | 关注社会议题与公共财政 | 超市采购、大件数码消费后扫小票 | 明确获知自己单次及累计为城市建设贡献了多少税款。 |
| **理性记账人群** | 习惯记录日常开销 | 日常小票快速录入 | 希望识别速度快、操作阻力小、分类清晰。 |
| **隐私极客 / 开发者** | 对数据安全与封禁敏感 | 使用开源/本地工具 | 杜绝云端隐私泄露，掌控数据所有权与 API 密钥。 |

---

## 三、 核心业务逻辑与计税算法

### 3.1 价税分离计算规范
所有零售小票实付金额均视为“价税合计（含税价）”，单项商品倒推公式如下：

$$不含税金额 = \frac{实付金额}{1 + 适用税率}$$

$$增值税额 = 不含税金额 \times 适用税率$$

- **精度处理**：所有金额在展示和入库时必须采用高精度定点数处理，统一四舍五入保留两位小数。
- **总额聚合**：整单税额为所有商品项计算后的税额之和，不得直接用整单实付总额乘以平均税率。

### 3.2 预设税率字典表
系统内置现行法定增值税分类标准：

| 税率代号 | 预设税率 | 适用商品范畴 | 识别关键词示例 |
| :--- | :--- | :--- | :--- |
| `STANDARD_13` | **13%** | 绝大多数工业制成品、加工食品、数码家电、日化洗护、服装鞋帽 | 薯片、可乐、纸巾、手机、沐浴露、洗洁精 |
| `AGRICULTURAL_9` | **9%** | 未经深加工的初级农林产品、粮食、食用植物油、鲜活肉蛋、图书报刊 | 大米、鲜猪肉、散装苹果、普通鸡蛋、书籍 |
| `EXEMPT_0` | **0%** | 部分大型商超直采自营的生鲜蔬菜、鲜活水产等政策性免税项 | 小票标有“F”、“免”字样的新鲜蔬菜 |
| `CUSTOM` | **自定义** | 允许用户手动输入任意有效税率值（0% ~ 100%） | 特殊服务类（如 6% 咨询/运输） |

---

## 四、 功能需求详细说明

### 4.1 数据录入模块 (Data Ingestion)

#### 4.1.1 快捷手动录入
- **输入字段**：
  - 商品名称（选填，为空时默认显示“消费品目”）
  - 实付金额（必填，限制为正浮点数）
  - 税率选择（必选，提供 13%、9%、0%、自定义 单选标签）
- **交互规范**：
  - 默认选中 13% 档位。
  - 支持“保存并继续添加”快捷交互，光标自动复位至金额输入框，降低多件录入阻力。

#### 4.1.2 视觉模型小票识别 (BYOK 模式)
- **图像前置处理**：
  - 支持系统相机实时拍摄或从相册选择。
  - 端侧必须在发起网络请求前进行尺寸等比裁剪压缩（长边限制在 1920px 以内，文件质量限制在 1MB 以内）。
- **接口调用规范**：
  - 客户端直接调用兼容 OpenAI 规范的 Chat Completions 端点。
  - 请求必须启用 **Tool Calling (Function Calling)** 模式，确保返回严格可解析的结构化数据。
- **Tool Calling Schema 定义**：
```json
{
  "type": "function",
  "function": {
    "name": "parse_receipt_tax_items",
    "description": "从购物小票中提取商户名、各项商品实付金额，并根据中国增值税税率规则进行分类",
    "parameters": {
      "type": "object",
      "properties": {
        "store_name": {
          "type": "string",
          "description": "商户/超市名称，无明显名称可返回空字符串"
        },
        "items": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "name": { "type": "string", "description": "商品品名简写" },
              "amount": { "type": "number", "description": "折后实付金额(元)" },
              "tax_rate": {
                "type": "number",
                "enum": [0.13, 0.09, 0.00],
                "description": "增值税率档位"
              },
              "category_reason": { "type": "string", "description": "归类简要理由" }
            },
            "required": ["name", "amount", "tax_rate"]
          }
        }
      },
      "required": ["items"]
    }
  }
}
```
- **系统提示词 (System Prompt)**：
  - 明确指令：过滤找零、折扣小计、会员卡号、欢迎标语、积分兑换等无关打印行。
  - 容错机制：遇到识别不清的模糊项，一律按 13% 标准税率处理，留待用户人工二次核验。

---

### 4.2 人工复核工作流 (Human-in-the-Loop)

AI 识别结果不得直接写入主账本，必须唤起“核对暂存界面”：
1. **列表式回显**：展示提取出的商户名称、预估总金额，以及商品明细列表。
2. **条目修正**：
   - 用户可直接点击修改品名、实付金额。
   - 提供快捷药丸按钮（Pill Button），一键在 13%、9%、0% 之间切换税率。
3. **行级剔除**：支持单项左滑删除或勾选批量清理误识别项。
4. **提交结算**：用户点击“确认入账”，客户端批量执行“价税分离”运算，原子化写入本地数据库。

---

### 4.3 看板与历史明细 (Dashboard & History)

#### 4.3.1 核心指标概览（顶部常驻）
- **累计贡献税额**：高亮大字号展示用户历史所有记录的增值税总和。
- **累计消费总支出**：历史实付总额汇总。
- **综合有效税负率**：$(累计税额 / 累计实付) \times 100\%$。

#### 4.3.2 历史账单流
- **卡片式瀑布流**：按消费时间倒序排列。
- **卡片摘要**：展示消费时间、商户名、商品项数量、本次实付总额、本次贡献税额。
- **详情弹窗/下钻页**：
  - 展开单笔账单的完整明细表：每项展示“品名、实付价、不含税价、税率、税额”。
  - 支持单笔账单二次编辑或整单物理删除（删除后动态重新计算全局指标）。

---

### 4.4 纳税人贡献卡生成与分享 (Taxpayer Certificate)

#### 4.4.1 卡片渲染规范
- **设计基调**：极简票据/纪念凭证风格（公文纸质底色或低饱和度 Slate 色系），突出秩序感与仪式感。
- **排版要素**：
  - **标题**：「纳税人公共贡献凭证」
  - **流水信息**：生成时间戳、商户名称、凭证编号（客户端本地 Hash 生成）。
  - **核心数值**：
    - 实付总金额：`¥XX.XX`
    - **本次贡献增值税：`¥XX.XX`**（核心加粗视觉焦点）
  - **税负构成进度条**：直观展示 13% 工业品税额与 9% 农产品税额的构成比例。
  - **底部寄语**：
    > “每一笔理性消费，都在为城市建设与公共事业注入力量。致敬每一位踏实的社会建设者。”

#### 4.4.2 导出能力
- 将卡片视图直接光栅化渲染为高清图片（PNG/JPEG）。
- 提供“保存到本地相册”与“唤起系统原生分享”双通道。

---

### 4.5 系统设置与数据安全 (Settings & Privacy)

#### 4.5.1 API 凭证管理 (BYOK)
- 配置字段：
  - `API Base URL`（如 `https://api.openai.com/v1` 或自定义中转端点）
  - `Model Name`（如多模态视觉模型代号）
  - `API Key`（必须使用平台级加密方案保存在客户端沙盒，界面默认密文展示，支持脱敏切换）
- 提供“网络与模型连通性测试”按钮，向模型发送轻量 Ping 请求以验证配置正确性。

#### 4.5.2 数据主权与自毁
- **零网络外流**：除请求用户填写的 API 域名外，应用内部严禁发起任何第三方数据上报。
- **数据导出/备份**：支持一键导出全量本地账本数据为标准 `JSON` 或 `CSV` 文件。
- **彻底抹除**：提供二次确认的安全抹除功能，一键物理清空本地数据库并重置加密密钥。

---

## 五、 数据结构设计 (Data Schema)

### 5.1 账单主体表 (`Receipt`)
| 字段名 | 类型 | 约束 | 描述 |
| :--- | :--- | :--- | :--- |
| `id` | String / Long | Primary Key | 账单唯一标识 (UUID 或自增 ID) |
| `store_name` | String | Nullable | 商户/超市名称 |
| `total_amount` | Decimal(10, 2) | Not Null | 本次消费实付总金额 |
| `total_tax_amount` | Decimal(10, 2) | Not Null | 本次消费增值税总税额 |
| `created_at` | Timestamp | Not Null | 记录创建时间戳 |

### 5.2 消费明细表 (`ReceiptItem`)
| 字段名 | 类型 | 约束 | 描述 |
| :--- | :--- | :--- | :--- |
| `id` | String / Long | Primary Key | 明细唯一标识 |
| `receipt_id` | String / Long | Foreign Key | 关联的主账单 ID (级联删除) |
| `name` | String | Not Null | 商品名称 |
| `amount` | Decimal(10, 2) | Not Null | 该项实付金额 (含税) |
| `tax_rate` | Decimal(4, 2) | Not Null | 适用税率 (如 0.13, 0.09) |
| `pre_tax_amount` | Decimal(10, 2) | Not Null | 该项不含税金额 |
| `tax_amount` | Decimal(10, 2) | Not Null | 该项贡献税额 |
| `category_reason` | String | Nullable | 归类理由或备注 |

### 5.3 用户配置模型 (`UserSettings`)
| 键名 | 存储形式 | 说明 |
| :--- | :--- | :--- |
| `api_base_url` | 本地持久化 | 模型请求根地址 |
| `model_name` | 本地持久化 | 调用的模型名称 |
| `api_key` | 平台硬件/系统加密存储 | 访问接口的鉴权密钥 |
| `default_tax_rate`| 本地持久化 | 手动录入时的默认预选税率 (默认 0.13) |

---

## 六、 非功能性需求 (NFR)

1. **完全离线可用**：在无网络环境下，纯手动录入、本地数据库读写、历史明细统计、分享卡片生成必须 100% 正常运行。
2. **端侧性能指标**：
   - 首页冷启动加载时间 < 800ms。
   - 本地计算与数据写入延迟 < 50ms。
   - 图片压缩处理时间 < 500ms。
3. **鲁棒性与异常保护**：
   - 面对褶皱、光线不足或 OCR 失败的小票，必须有明确的降级提示，引导用户手动补录，严禁 App 崩溃。
   - 视觉模型返回非法 JSON 或参数缺失时，能自动容错拦截并转为手工填单模式。

---

## 七、 实施阶段规划 (Milestones)

- **Phase 1 (核心计算与离线闭环)**：
  - 交付本地数据库、计税计算引擎、手动录入流程、历史看板及贡献卡导出功能。
- **Phase 2 (AI 视觉接入与工作流整合)**：
  - 交付 BYOK 设置中心、图片端侧压缩器、多模态 Tool Calling 联调、人工复核抽屉界面。
- **Phase 3 (体验与数据迁移)**：
  - 完善本地 JSON/CSV 数据导出导入、完善多套精致分享卡模版、优化极端场景容错。